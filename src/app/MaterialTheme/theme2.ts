import { createTheme } from '@material-ui/core/styles';

const theme2 = createTheme({
  palette: {
    primary: {
      main: "#ff9619",
    },
    secondary: {
      main: "#423127",
    },
  },
});

export default theme2;